# Simulator Agent Instructions

Scope: `simulator/`

Read the root `AGENTS.md` and `docs/IOT.md`.

Generate realistic but configurable readings. Support deterministic test mode, stable message/event identifiers, multiple devices/sensors, stale/offline devices, and invalid readings. Do not embed business automation logic in the simulator: it produces data, while the application decides what that data means.
